import { Base } from '../base';
import { accountBalance } from '../schemas';
import { AccountBalancesParams } from './types';
export declare class AccountBalances extends Base {
    getAccountBalances(date: string, params?: AccountBalancesParams): Promise<accountBalance[]>;
    getAccountBalance(accountCode: string, date: string): Promise<accountBalance>;
}
