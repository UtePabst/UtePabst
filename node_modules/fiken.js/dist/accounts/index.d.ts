import { Base } from '../base';
import { account } from '../schemas';
import { AccountsParams } from './types';
export declare class Accounts extends Base {
    getAccounts(params?: AccountsParams): Promise<account[]>;
    getAccount(accountCode: string): Promise<account>;
}
