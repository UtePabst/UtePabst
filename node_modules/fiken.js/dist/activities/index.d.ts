import { Pagination } from '../base';
import { Base } from '../base';
import { activityRequest, activityResult, updateActivityRequest } from '../schemas';
type ActivitiesParams = Pagination & {
    name?: string;
    archived?: boolean;
};
export declare class Activities extends Base {
    getActivities(params?: ActivitiesParams): Promise<activityResult[]>;
    createActivity(activity: activityRequest): Promise<void>;
    getActivity(activityId: number): Promise<activityResult>;
    updateActivity(activityId: number, activity: updateActivityRequest): Promise<void>;
    deleteActivity(activityId: number): Promise<void>;
}
export {};
