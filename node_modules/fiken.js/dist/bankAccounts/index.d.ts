import { Base } from '../base';
import { bankAccountRequest, bankAccountResult } from '../schemas';
import { BankAccountsParams } from './types';
export declare class BankAccounts extends Base {
    getBankAccounts(params?: BankAccountsParams): Promise<bankAccountResult[]>;
    getBankAccount(bankAccountId: number): Promise<bankAccountResult>;
    createBankAccount(params: bankAccountRequest): Promise<void>;
}
