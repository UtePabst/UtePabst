import { Pagination } from '../base';
import { Base } from '../base';
import { bankBalanceResult } from '../schemas';
type BankBalancesParams = Pagination & {
    date?: string;
};
export declare class BankBalances extends Base {
    getBankBalances(params?: BankBalancesParams): Promise<bankBalanceResult[]>;
}
export {};
