import { Base } from '../base';
import { contact, contactPerson } from '../schemas';
import { Pagination } from '../base';
type ContactsParams = Pagination & {
    lastModified?: string;
    lastModifiedLe?: string;
    lastModifiedLt?: string;
    lastModifiedGe?: string;
    lastModifiedGt?: string;
    createdDate?: string;
    createdDateLe?: string;
    createdDateLt?: string;
    createdDateGe?: string;
    createdDateGt?: string;
    supplierNumber?: number;
    customerNumber?: number;
    organizationNumber?: string;
    memberNumber?: number;
    memberNumberString?: string;
    name?: string;
    email?: string;
    customer?: boolean;
    supplier?: boolean;
    inactive?: boolean;
    group?: string;
    sortBy?: string;
};
export declare class Contacts extends Base {
    getContacts(params?: ContactsParams): Promise<contact[]>;
    createContact(contact: contact): Promise<contact>;
    getContact(contactId: number): Promise<contact>;
    updateContact(contactId: number, updatedContact: contact): Promise<contact>;
    deleteContact(contactId: number): Promise<contact>;
    getContactPersons(contactId: number): Promise<contactPerson[]>;
    addAttachmentToContact(contactId: number, formData: FormData): Promise<void>;
    createContactPersonAttachment(contactId: number, formData: FormData): Promise<void>;
    getContactPerson(contactId: number, contactPersonId: number): Promise<contactPerson>;
    createContactPerson(contactId: number, contactPerson: contactPerson): Promise<void>;
    updateContactPerson(contactId: number, contactPersonId: number, contactPerson: contactPerson): Promise<void>;
    deleteContactPerson(contactId: number, contactPersonId: number): Promise<void>;
}
export {};
