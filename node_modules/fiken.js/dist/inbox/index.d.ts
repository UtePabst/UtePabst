import { Base } from '../base';
import { Pagination } from '../base';
import { inboxResult } from '../schemas';
type InboxParams = Pagination & {
    sortBy?: 'createdDate asc' | 'createdDate desc' | 'name asc' | 'name desc';
    status?: 'all' | 'unused' | 'used';
    name?: string;
};
export declare class Inbox extends Base {
    getInbox(params?: InboxParams): Promise<inboxResult[]>;
    createInbox(formData: FormData): Promise<void>;
    getInboxDocument(inboxDocumentId: number): Promise<inboxResult>;
}
export {};
