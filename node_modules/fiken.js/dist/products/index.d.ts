import { Base } from '../base';
import { product, productSalesReportRequest, productSalesReportResult } from '../schemas';
import { ProductsParams } from './types';
export declare class Products extends Base {
    getProducts(params?: ProductsParams): Promise<product[]>;
    createProduct(product: product): Promise<void>;
    getProduct(productId: number): Promise<product>;
    updateProduct(productId: number, updatedProduct: product): Promise<void>;
    deleteProduct(productId: number): Promise<void>;
    salesReport(productSalesReportReq: productSalesReportRequest): Promise<productSalesReportResult>;
}
