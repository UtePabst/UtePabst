import { Base } from '../base';
import { Pagination } from '../base';
import { projectRequest, projectResult, updateProjectRequest } from '../schemas';
type ProjectsParams = Pagination & {
    completed?: boolean;
    name?: string;
    number?: string;
};
export declare class Projects extends Base {
    getProjects(params?: ProjectsParams): Promise<projectResult[]>;
    createProject(project: projectRequest): Promise<void>;
    getProject(projectId: number): Promise<projectResult>;
    updateProject(projectId: number, project: updateProjectRequest): Promise<void>;
    deleteProject(projectId: number): Promise<void>;
}
export {};
