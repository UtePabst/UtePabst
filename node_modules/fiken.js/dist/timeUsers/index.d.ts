import { Pagination } from '../base';
import { Base } from '../base';
import { timeUserResult } from '../schemas';
type TimeUsersParams = Pagination & {
    name?: string;
    email?: string;
};
export declare class TimeUsers extends Base {
    getTimeUsers(params?: TimeUsersParams): Promise<timeUserResult[]>;
    getTimeUser(timeUserId: number): Promise<timeUserResult>;
}
export {};
